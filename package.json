{
  "name": "shepherdcheck",
  "version": "1.0.0",
  "description": "ShepherdCheck — Church Kids Check-In System",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/index.js"
  }
}